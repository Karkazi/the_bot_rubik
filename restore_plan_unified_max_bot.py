# -*- coding: utf-8 -*-
"""
Restore PLAN_UNIFIED_MAX_BOT.md to full Cyrillic UTF-8.
Builds content in parts, then appends section 11 from PLAN_UNIFIED_MAX_BOT_APPEND.md.
"""
import os

SCRIPT_DIR = os.path.dirname(os.path.abspath(__file__))
TEMP_FILE = os.path.join(SCRIPT_DIR, "plan_restored_content.txt")
APPEND_FILE = os.path.join(SCRIPT_DIR, "PLAN_UNIFIED_MAX_BOT_APPEND.md")
OUT_FILE = os.path.join(SCRIPT_DIR, "PLAN_UNIFIED_MAX_BOT.md")

# --- Section 6.7a: keep exact current text (from file) ---
SECTION_6_7A = r"""### 6.7a Синхронизация регистрации Telegram и MAX: как узнать пользователя в другом канале

Цель: один и тот же человек, зарегистрировавшийся в одном канале (Telegram или MAX), при первом входе в другой канал должен быть распознан и привязан к **одному профилю** (общие «Мои заявки», один набор данных). Ниже — правила привязки по каналам.

#### Единый идентификатор в Core

- В Core храним **профиль** по внутреннему **support_user_id** (UUID или автоинкремент). В профиле: `support_user_id`, `telegram_user_id` (опционально), `max_user_id` (опционально), плюс бизнес-поля (ФИО, телефон, email, логин, department и т.д.).
- **Индексы:** по `telegram_user_id`, по `max_user_id`, по `phone` (нормализованный), по `email`, по `login`. Это нужно для поиска профиля при привязке из другого канала.

#### Сценарий 1: Регистрация в MAX → вход в Telegram

1. Пользователь открывает бота (WMS/Lupa/Rubik) в Telegram. Core ищет профиль по `telegram_user_id`. **Не найден** → «первый вход в Telegram».
2. Показываем выбор: **«Привязать аккаунт»** / **«Зарегистрироваться»**.
3. **«Привязать аккаунт»:** запрашиваем **только телефон** — кнопка «Поделиться контактом» (ввод номера с клавиатуры не принимаем). Core ищет профиль по `phone`. Если найден профиль с `max_user_id` и без `telegram_user_id` → дописываем в этот профиль `telegram_user_id`. Сообщение: *«Аккаунт привязан.»* Дальше — общий профиль и общие заявки.
4. Если по телефону профиль не найден — сообщение *«Аккаунт не найден. Зарегистрируйтесь или проверьте номер.»* и снова выбор: привязать / зарегистрироваться.
5. **«Зарегистрироваться»** — полная регистрация (6 шагов). На шаге «телефон» при дубликате номера предлагаем привязать этот Telegram к существующему аккаунту.

#### Сценарий 2: Регистрация в Telegram → вход в MAX

1. Пользователь открывает бота поддержки в MAX. Core ищет профиль по `max_user_id`. **Не найден** → «первый вход в MAX».
2. **Привязать:** только **одноразовый код из Telegram**. В MAX показываем: *«Введите 6-значный код из Telegram-бота поддержки»*. Пользователь в Telegram нажимает «Привязать MAX» → бот выдаёт код (6 цифр), действителен 2–5 минут; код сохраняем в кэш: `code → (support_user_id, telegram_user_id)`. Пользователь вводит код в MAX → Core по коду находит профиль и дописывает `max_user_id`. Email и телефон в MAX не запрашиваем.
3. Если пользователь выбирает «Зарегистрироваться» в MAX — запускаем регистрацию; при совпадении телефона/email с существующим профилем предлагаем привязку.

#### Реестр привязок и уведомления

- «Мои заявки» и уведомления строим по `support_user_id` (все привязанные channel_user_id — telegram и/или max). Политику доставки уведомлений (в оба канала или только канал создания) задаём в конфиге.

#### Краткая сводка

| Ситуация | Как привязать |
|----------|---------------|
| Зарегистрирован в MAX → зашёл в Telegram | **Телефон** — только «Поделиться контактом»; поиск профиля по phone; если найден с max_user_id и без telegram_user_id → дописать telegram_user_id. |
| Зарегистрирован в Telegram → зашёл в MAX | **Одноразовый код из Telegram** (кнопка «Привязать MAX» → 6-значный код, 2–5 мин) → ввод кода в MAX → по коду находим профиль и дописываем max_user_id. Без ввода email/телефона в MAX. |
"""


def write_part(path, content, mode="w"):
    with open(path, mode, encoding="utf-8") as f:
        f.write(content)


def main():
    # Part 1: header + intro + section 1
    part1 = """Восстановлено: 2025-02-24. Полный план в UTF-8.

# План объединения support-ботов (WMS, Lupa, Rubik) в Core + адаптеры (MAX/Telegram)

Единая архитектура: один общий support-бот, доступный в MAX и Telegram, с единой регистрацией по шагам и заявками в Jira по каталогу тикетов (в т.ч. смена пароля и т.п.). Это даёт «единое окно» для пользователей, единый каталог тикетов и единые уведомления в оба канала. Три бота-клиента в Core, в адаптерах (MAX/Telegram) только преобразование ввода-вывода в формат канала.

**Цель:** объединить WMS, Lupa и Rubik в единый **Support Core** (ядро) с доступом из двух каналов: **MAX** и **Telegram**. Пользовательский опыт должен быть **единым меню** тикетов. В Core хранятся каталог и реестр заявок (в т.ч. типы заявок в Jira из раздела 5, но не только 50).

> Примечание: один человек может писать и в тот и в другой канал MAX и Telegram. Идентификация пользователя dute и привязка заявок в таком случае **не дублируются**.

**Три канала доступа (кратко по текущему):**
- **Telegram:** в Lupa, WMS и Rubik уже работают **три бота**, зарегистрированные в трёх приложениях. Все три бота в Telegram обращаются к одному меню тикетов и в то же Core, но пока не объединены в (или мигрированы в) единый каталог тикетов/меню по «линейке» продуктов (WMS / Lupa / Rubik).
- **MAX:** в MAX сейчас один общий вид **единого бота поддержки** (один токен). Единое меню доступно в MAX: «Создать заявку» (или аналог по названию), «Мои заявки», смена пароля, логин и все те же типы заявок что и там.

**Текущие репозитории:**
- `the_bot_wms` — заявки по складу/WMS (только Telegram)
- `the_bot_lupa` — заявки по поиску petrovich.ru (только Telegram)
- `the_bot_rubik` — смена пароля, логин, Jira AA (только Telegram)

---

## 1. Архитектура: Core и адаптеры

### 1.1 Core как ядро support-ботов

Всё общее живёт в **Core** (ядро):

- **Support (ядро):** регистрация, профили (department, wms_sd_department и т.д.), **каталог типов заявок** (см. п. 1.3), создание заявок в Jira (по типам заявок/проектам/полям), реестр заявок, уведомления, комментарии, логика по продуктам (WMS, Lupa, Rubik и будущие линейки).

Core не зависит от канала: ни от Telegram ни от MAX. Он отдаёт «меню» (регистрация + каталог заявок + прочее) и принимает «ответы» (какой тип заявки, что заполнено, данные пользователя). Адаптеры Telegram/MAX только вызывают API Core (получают меню, отправляют данные и т.д. для UI).

В Core хранится **реестр с привязкой заявок** для всех сценариев:
- создание заявки в канал и привязка (какой пользователь какую заявку создал, какой тип/какой проект в Jira);
- уведомления по заявкам пользователю;
- хранение по заявкам в реестре, кто создал, статусы и т.д.

### 1.2 Адаптеры и два канала Core

**Адаптеры** в этой схеме подключаются к Core: получают меню с сервера, отдают его в Core, получают ответ в формате для отображения в канале.

| Адаптер | Назначение |
|---------|------------|
| **MAX** | **Один токен** (один бот): единый бот поддержки для доступа в MAX. Все сценарии (заявки, смена пароля, «Мои заявки») идут через Support Core и отображаются в виде меню/кнопок/форм. Один токен на все продукты по каталогу (WMS, Lupa, Rubik и т.д.). |
| **Telegram** | **Три бота** (три приложения в WMS, Lupa, Rubik): единое меню для регистрации и создания заявок. Все три бота обращаются к одному Support Core; при миграции в единый каталог каждый бот показывает «линейку» типов заявок по продукту (WMS / Lupa / Rubik). |

В MAX (один токен), в Telegram (три токена) отображается **меню и реестр заявок** (то, что вычисляется в Core), а преобразование ввода/вывода делается в адаптере (кнопки, формат сообщений и т.д.).

### 1.3 Каталог типов заявок в Core (ориентировочно 50+ типов)

Сейчас три бота WMS, Lupa и Rubik создают **5 типов заявок** в Jira. В плане же — **свыше 50 типов заявок**, в одном каталоге, в едином реестре. Core должен уметь создавать заявки по этому каталогу.

**Принцип:** каждый тип заявки описывается **каталогом**, а не зашит в код.

- **Каталог типов заявок** в файле (JSON/YAML/и т.д.): для каждого типа указываются:
  - идентификатор типа (например, `wms_ticket`, `lupa_search`, `rubik_password`, `tp_network`, `tp_access` и т.д.);
  - проект Jira (key);
  - тип задачи Jira (issue type);
  - поля формы заявки: какие поля запрашиваются и куда в каком поле Jira мапятся (в т.ч. поля типа: department, wms_sd_department, подразделение и т.д.);
  - уведомления: уведомлять ли пользователя (создание, комментарии, статус, смена и т.д. в Lupa/WMS);
  - уведомления: доставка по каналу (MAX/Telegram).

- **Единый реестр типов заявок** = каталог заявок в конфиге + все связанные с ним данные. Для Core важно: «по какому типу заявки создаём задачу в реестре и в каком issue в Jira». Создание заявки идёт по каталогу (тип, проект, какой тип/какой проект).

- **Первые 5 типов** (WMS, Lupa, Rubik) уже описанные сценарии и конфигурации; остальные 45+ типов в каталоге пока не детализированы — по мере переноса будут добавляться в каталог и реестр.

Итог раздела: **единый каталог и единый реестр заявок**. В любом канале пользователь выбирает тип заявки из каталога и создаёт заявку / смотрит реестр в Jira. Различие department vs WMS SD Department в каталоге задаётся по полям, а не дублируется в коде.

---

"""
    write_part(TEMP_FILE, part1)

    # Part 2: section 2
    part2 = """## 2. Текущие три бота (сводка)

Три репозитория сейчас работают по отдельности. Их логика переносится в Core и адаптеры по единому каталогу.

### WMS (the_bot_wms)

| Компонент | Описание в репозитории |
|-----------|------------------------|
| **Точка входа** | `main.py` с `setup_bot()`, `dp.start_polling(bot)`. Только Telegram. |
| **Регистрация** | `src/bot/handlers/registration.py`, FSM: full_name → phone → email → department (из `WMS_DEPARTMENTS`) и подтверждение. Хранение: `data/registration_requests.json`, затем запись в `users.json` (name, phone, role, department, **department_key**). |
| **Подразделение в Jira** | **customfield_18215** с отображением = отображаемое название (справочник «подразделение»); в коде `jira.custom_fields.department`. Другие поля: process (customfield_13803), service_type (customfield_10500), wms_settings (customfield_18402) и т.д. |
| **Проект / тип заявки** | Проект **PW** (config["project"]["key"]). Типы: wms_issue (заявка по настройке WMS), wms_settings (настройки WMS), psi_user (подключение PSIwms). JSM: service_desk_id, request_type_id. |
| **«Мои заявки»** | JQL: `project = PW AND labels = "поддержка" AND status NOT IN (Closed, Resolved, Done, Rejected)`. **Привязка идёт не по reporter**, а по **совпадению в description**: «Контактное лицо: {name}, {phone}» по шаблону; заявки считаются принадлежащими по этому полю. |
| **Уведомления** | `src/services/notification_service.py`: цикл раз в **20 с** (config["bot"]["notifications"]["check_interval_seconds"]). По JQL находит открытые заявки в статусе «поддержка»; для каждой заявки ищет user_id по имени и description; при новом комментарии/изменении статуса (кроме как в текущем канале "[Telegram-бот]") отправляет в Telegram chat_id = user_id. |
| **Админка** | `config["admin"]["main_admin_id"]` плюс роль `admin` в users.json. Функции: просмотр заявок, создание/редактирование/подтверждение, пароль (только main_admin), доступ по подразделению, логи, смена подразделений. |

### Lupa (the_bot_lupa)

| Компонент | Описание в репозитории |
|-----------|------------------------|
| **Точка входа** | `main.py`, только Telegram. |
| **Регистрация** | FSM: FIO → email → subdivision (справочник из config) и табельный номер вручную. Хранение: **data/user_data.json** (full_name, email, subdivision, employee_id, phone). |
| **Тип по проекту** | Проект **WHD**, тип **Incident**. Поля: problematic_service (customfield_12312), request_type (customfield_15800), subdivision (customfield_11406), service (customfield_10500), address_city (customfield_12403). Логика: приложение (поиск/сайт) и т.д. описание в полях и комментариях. |
| **«Мои заявки»** | **Не по reporter.** Привязка: при создании заявки в **data/tasks_users.json** пишется `issue_key → telegram_id`. Справочник «мои» в том же файле tasks_users.json и value = соответствующий user_id; по ключу находят issue, при Done/Resolved/Closed отправляют уведомление. |
| **Уведомления** | Отдельный цикл раз в **20 с**: JQL по WHD + labels "поиск" + не закрытые; для каждой заявки ищут user_id по tasks_users.json; при новом комментарии/изменении статуса (текст сообщения не из бота) отправляют в Telegram. Кнопка «Ответить» с callback quick_reply_comment:{issue_key}. |
| **Админка** | Доступ по роли в **data/jira_tickets_log.xlsx**. Конфиг: data/admins.json и переменная окружения OWNER_ADMIN и access_control.py. |

### Rubik (the_bot_rubik)

| Компонент | Описание в репозитории |
|-----------|------------------------|
| **Точка входа** | `main.py`, только Telegram. Support-ядро в **core/** (registration, password, jira_aa, password_requests) и вызовы в handlers по сценариям. |
| **Регистрация** | FIO и рабочий логин (i.ivanov) и т.д. email (@petrovich.ru / .tech) и Department (из Jira createmeta) и подтверждение. Поиск по логину/email дубликатов в user_storage. Хранение: **data/user_data.json** + индексы index_by_login, index_by_email, **index_by_phone** (для привязки MAX). |
| **Тип заявки** | Отдельная заявка в **Jira AA**: project AA, Service Desk API (requestTypeId «Смена пароля»), поля AD account, existing phone, new password, department (JIRA_AA_FIELD_DEPARTMENT / customfield_11406 и т.д.). |
| **Реестр заявки и уведомления** | **data/pending_password_requests.json** (issue_key → user_id). При переходе в Resolved/Rejected заявку удаляют. |
| **Уведомления** | Два цикла: (1) **PASSWORD_STATUS_CHECK_INTERVAL** (по умолчанию 90 с) и проверка статуса заявки по реестру; при Resolved — «Пароль успешно изменён», при Rejected — «В заявке отказано». (2) **COMMENTS_CHECK_INTERVAL** (30 с) и проверка комментариев по заявке и отправка уведомлений + кнопка «Добавить комментарий». |
| **Админка** | ADMIN_IDS из .env. Расширенные права: смена подразделения (по Telegram ID или по логину, смена/выдача по паролю). |
| **MAX** | В этом репозитории уже предусмотрена привязка по телефону (`user_storage.find_by_phone()`); при входе в MAX пользователь вводит номер телефона; поиск user_id по номеру и привязка идёт через core.registration / core.password. |

### Сводка: Jira и привязка заявок по продуктам

| Продукт | Проект Jira | Тип задачи | Способ «мои заявки» / уведомления |
|---------|-------------|------------|----------------------------------|
| WMS | PW | Заявка/настройка (JSM) | Совпадение в description «Контактное лицо: {name}, {phone}» + label «поддержка». |
| Lupa | WHD | Incident | tasks_users.json: issue_key → telegram_id. |
| Rubik | AA | Тип JSM (смена пароля) | pending_password_requests.json: issue_key → user_id. |

В Core Support нужен **единый реестр привязки заявок**: при создании заявки пишем (channel_user_id, issue_key, project, type) и по нему ведём; «мои заявки» и уведомления строятся по этому реестру, а не по разным способам привязки (описание, отдельные JSON) в каждом боте.

### 2.1 Регистрация по каналам: шесть шагов обязательных (единый минимум)

Все зарегистрированные пользователи должны **пройти шесть шагов в порядке заполнения профиля**, и тогда достигается 100% совместимость данных между продуктами. Для совместимости Support Core задаём такой минимум полей: полное имя + контактные данные (WMS/Lupa/Rubik).

#### Поля общие (для Support Core, заполняются по шагам)

- **Идентификаторы каналов (для привязки заявок и уведомлений)**:
  - `max_user_id` (ид пользователя в MAX)
  - `telegram_user_id` (ид пользователя в Telegram)
- **Данные:**
  - `full_name` (ФИО)
  - `phone` (телефон; и в WMS обязателен, и в Rubik «existing phone», и в остальных)
  - `email` (корпоративный, **строго** `@petrovich.ru` или `@petrovich.tech` в рамках текущих правил для всех ботов/продуктов)

#### Поля, обязательные для WMS (the_bot_wms)

- **Обязательные:**
  - `full_name`
  - `phone`
  - `email` (корпоративный: `@petrovich.ru` / `@petrovich.tech`)
  - `wms_department_display` и отображаемое название отдела/подразделения (маппинг в Jira **customfield_18215**)
  - `wms_department_key` — ключ из справочника `WMS_DEPARTMENTS` (тот же что и при создании заявки/настройке)
- **Привязка по первой заявке WMS:**
  - «Мои заявки» и уведомления строятся по совпадению в description: `Контактное лицо: {name}, {phone}`. Для переноса в Core можно не менять по проекту, но **phone и full_name** уже есть в общем профиле для привязки.

#### Поля, обязательные для Lupa (the_bot_lupa)

- **Обязательные:**
  - `full_name`
  - `email` (корпоративный: `@petrovich.ru` / `@petrovich.tech`)
  - `lupa_subdivision` — подразделение (маппинг в Jira **customfield_11406**)
  - `lupa_employee_id` — табельный номер (для отчётов/подтверждения в Excel и т.д.)
  - `phone`
- **Опционально/дополнительно:**
  - (опционально) привязка/подтверждение по табельному номеру, если требуется в процессе

#### Поля, обязательные для Rubik (the_bot_rubik)

- **Обязательные:**
  - `full_name`
  - `rubik_work_login` — рабочий логин (i.ivanov); по нему же AD account в заявке
  - `rubik_corporate_email` — корпоративный email (`@petrovich.ru` / `@petrovich.tech`), в т.ч. обязательный
  - `rubik_department` — department из Jira (AA, поле `JIRA_AA_FIELD_DEPARTMENT`, например customfield_11406)
  - `phone` — нормализованный для existing phone в заявке
- **Дополнительно:**
  - шифрование персональных полей по желанию (в текущем Rubik есть `ENCRYPT_USER_DATA` / `USER_DATA_ENCRYPTION_KEY`)

#### Шесть шагов регистрации (общие)

Все обязательные поля заполняются по шагам **в таком порядке**:

1. **ФИО** в свободной форме (полное имя, фамилия, имя). Проверка: минимальная длина и кириллица.
2. **Телефон** с обязательной **кнопкой «Поделиться контактом»** в чате (как в WMS «контактное лицо»), иначе нельзя продолжить; если в адаптере нет контакта — по возможности не допускать перехода на следующий шаг (или требовать ввод с проверкой формата).
3. **Логин** в формате `i.ivanov` (рабочий логин / AD account). **Важно:** логин должен быть **уникальным**. Если логин занят — сообщение об ошибке; если пользователь не сотрудник — отображаем: *«Регистрация в support доступна только сотрудникам. Обратитесь к администратору для уточнения.»* и останавливаем на этом шаге.
4. **Корпоративный email** в домене компании (`@petrovich.ru` или `@petrovich.tech`). **Важно:** домен должен быть **корпоративным**. Если email не корпоративный — отображаем: *«Регистрация в support корпоративной почты доступна только сотрудникам. Обратитесь к администратору для уточнения.»* и останавливаем на этом шаге.
5. **Табельный номер** как в Lupa (число/подразделение, при необходимости проверка). Проверка обязательна, что **номер принадлежит сотруднику** (как в текущем потоке Lupa). Если табельный не совпадает — показываем ошибку и ссылку **«Обратиться в поддержку Lupa»** (альтернатива по текущей логике Lupa).
6. **DEPARTMENTS (подразделение)** — выбор из справочника по Jira; отображаем **справочник-каталог с поиском по первым 12 символам названия** (кнопки «Далее» / «Назад» для постраничной навигации). Обязательное поле заполняется в конце и маппится в нужное поле (либо department, используемое в Rubik/Jira AA и т.д. в Lupa; см. п. 2.2 про WMS_DEPARTMENTS отдельно).

Все шаги 6 обязательных выполняются; порядок фиксирован. **WMS-специфичные справочники (WMS_DEPARTMENTS)** подгружаются отдельно и показываются только при первом создании заявки типа WMS (см. п. 2.2).

#### Исключения по продуктам при регистрации в Support Core (кратко)

- **Порядок полей регистрации** задаётся 6 шагов по этому списку (ФИО, телефон, логин, email, табельный номер и DEPARTMENTS). WMS_DEPARTMENTS только при первом типе заявки WMS (п. 2.2).
- **Валидация полей:** логин и email проверяются на уникальность; при дубликате в том же продукте — ошибка. Остальные поля по правилам (как в Lupa).
- **Подгрузка справочников по продукту:** если пользователь выбрал тип заявки для WMS / Lupa / Rubik и в этом сценарии нужен какой-то доп. справочник — Core подгружает его (например, WMS_DEPARTMENTS при первом типе заявки WMS).

#### 2.2 WMS_DEPARTMENTS: загрузка при первом типе заявки WMS и только тогда

Для WMS справочник (**WMS_DEPARTMENTS**) не подгружается при общей регистрации. Правила такие:

1. **Общая регистрация:** выполняются все 6 шагов (ФИО, телефон, логин, email, табельный номер, DEPARTMENTS и т.д. по общему списку). Поля `wms_department_key` и `wms_department_display` в профиле заполняются **только при создании** (т.е. DEPARTMENTS из Jira на шаге 6 и общие справочники для Rubik/Lupa; WMS-специфичный справочник подгружается отдельно при первом создании заявки WMS).
2. **Первый тип заявки в продукте WMS:** при нажатии «Создать заявку» и выборе типа WMS, Core выполняет подгрузку: запрашивает **список отделов первого уровня** справочника для:
   - загрузка списка **WMS_DEPARTMENTS** (из Jira или из конфига/админки);
   - отображение кнопок-справочника в интерфейсе отдела/подразделения;
   - заполнение полей профиля в этом сценарии (`wms_department_key`, `wms_department_display`) и отображение названия отдела (маппинг в нужное поле Jira, например customfield_18215).
3. **Остальные типы WMS:** Core кэширует WMS_DEPARTMENTS по продукту, обновление по расписанию.
4. **Резервный вариант (ручная выдача / админка):** предусмотреть возможность задать справочник WMS-отделов вручную (например, через конфиг в репо или админку). Тогда первый бот WMS работает так же и в том же реестре.

Итог: WMS_DEPARTMENTS подгружается только при первом типе заявки WMS (не при общей регистрации), хранится в кэше и в этом сценарии используется только для WMS; для остальных продуктов общие справочники заполняются на шаге 6 регистрации.

---

"""
    write_part(TEMP_FILE, part2, "a")

    # Part 3: section 3
    part3 = """## 3. Структура проекта (Core + адаптеры)

### 3.1 Дерево каталогов: core и два адаптера

```
project/
├── core/                           # ядро support-ботов, не зависит от канала
│   ├── support/                    # модули поддержки
│   │   ├── user_profile.py        # профиль: department, department_key, department_display, и т.д.
│   │   ├── ticket_catalog.py      # каталог типов заявок (типы + маппинг)
│   │   ├── ticket_creation.py     # создание заявки по типу из каталога
│   │   ├── issue_binding_registry.py  # реестр привязки (channel_id, user_id, issue_key, project) для «мои заявки» и уведомлений
│   │   ├── jira_client.py         # общий клиент Jira; создание issue/тип/поля по каталогу
│   │   ├── my_tickets.py          # «мои заявки», отображение (по реестру привязки)
│   │   ├── notifications.py      # опрос статусов Jira, уведомления (по реестру)
│   │   ├── admins.py              # проверка по админам (WMS, Lupa, Rubik, и т.д.)
│   ├── api.py                     # единый API Core: вызовы для адаптеров (create_ticket, get_menu, ...)
│   ├── config_core.py             # конфигурация ядра Core (каталог заявок, Jira, уведомления)
│
├── adapters/                      # каналы к Core
│   ├── max/                       # бот в MAX (один токен и единое меню тикетов)
│   │   ├── main_max.py            # точка входа: polling
│   │   ├── support_handlers.py    # обработчики вызовов core.support (не дублируют логику)
│   │   ├── config_max.py          # MAX_BOT_TOKEN
│   ├── telegram/                  # три бота в Telegram (WMS, Lupa, Rubik в одном меню)
│   │   ├── main_tg.py             # запуск по одному токену (или три точки входа)
│   │   ├── support_handlers.py
│   │   ├── config_tg.py           # TELEGRAM_TOKEN_WMS / LUPA / RUBIK
│
├── config/
│   ├── ticket_catalog.yaml        # каталог типов заявок (50+): id, project, issue_type, fields, уведомления
│   ├── support_profile_fields.yaml # поля профиля (department, wms_sd_department, и т.д.) и маппинг в Jira
│   ├── .env.example
├── data/
│   ├── users_support.json
│   ├── admins_*.json
│   ├── ...
├── requirements.txt
```

 - **Адаптеры** по определению тонкие: только преобразование формата ввода-вывода в формат Core и обратно (меню, формы, «мои заявки»). Вся логика «создать заявку» и т.п. идёт через Core (единый каталог) и не дублируется по каналам (кнопки, текст в Telegram/MAX).
- **Регистрация/профиль пользователя** в Core (как и заявки, так и профиль в реестре) хранится единообразно; адаптеры только отправляют данные и получают ответы (на свой формат).

### 3.2 По запуску сервисов: отдельно Core + MAX и Telegram

По отдельности запускаются сервисы адаптеров `adapters/max` и `adapters/telegram`. Оба адаптера вызывают один и тот же `core.api`, т.е. support-ядро Core не зависит от канала вызова.

### 3.3 Токенизация

- **Токен MAX (support-бот):** один `MAX_BOT_TOKEN` (получается от администрации). В MAX отображается один общий бот для доступа во все продукты и все типы заявок.
- **Токены Telegram:** три штуки в `TELEGRAM_TOKEN_WMS`, `TELEGRAM_TOKEN_LUPA`, `TELEGRAM_TOKEN_RUBIK` (три приложения в .env). Запуск/поллинг для каждого бота в отдельном long poll-процессе и не блокируют друг друга; единое меню тикетов в каждом боте и в «линейке» продуктов (WMS / Lupa / Rubik).
- **Core:** читает каталог заявок (см. п. 1.3), логику Support (подключение Jira, уведомления, реестр заявок по типам Jira по каталогу). Конфиг из `.env` или отдельного config.

---

"""
    write_part(TEMP_FILE, part3, "a")

    # Part 4: sections 4, 5
    part4 = """## 4. Меню типов заявок в одном «Создать заявку»

- **Единое меню:** `/start` в MAX или Telegram показывает одно и то же меню:
  - **«Создать заявку»** с отображением **по каталогу типов заявок** в Core (первые 5 типов: WMS, Lupa, Rubik и т.д.; при расширении в том же каталоге до 50+). В зависимости от выбора пользователь видит тип заявки и соответствующие поля (например, для WMS — тогда WMS-справочники и поля по продукту из Jira, справочники), заполняет и отправляет в Core в едином формате регистрации (см. п. 2.2).
  - **«Мои заявки»** с отображением списка по реестру/каналу (WMS, пароль, смена/доступ и т.д. по каталогу).
  - **«Создать заявку» / «Мои заявки»** с подменю по продуктам: WMS, пароль, смена/доступ и т.д. И обязательно подгружаем **WMS-справочники** (WMS_DEPARTMENTS), справочники Lupa, типы Rubik и т.д. При создании заявки заполняем WMS-справочники (например, при первом типе заявки WMS).
  - **Пароль** (логин + по запросу).
  - **Админка** (если права есть: просмотр заявок и т.д. по продукту и по реестру привязки, если есть).

Обработка по callback-кнопкам по каталогу типов заявок; все support и все типы заявок идут по каталогу (например, `ticket_wms_`, `ticket_lupa_`, `ticket_rubik_`, `ticket_tp_*` или другие id из каталога). Логика «Создать заявку» вызывается по каталогу типов заявок (в том числе не по продукту, если нужно).

---

## 5. Сценарий приёмки (обязательный минимум)

Ниже в сценарии проверяем только то, что объединённый бот создаёт заявки и привязывает уведомления: создание двух заявок (WMS + ещё одна) и проверка в реестре и уведомлениях.

---

### 5.1 Сценарий: создание заявки в WMS и в другом продукте (Lupa), привязка и уведомления

**Цель:** приёмка сценария. Канал: MAX или Telegram. Шаги: создать две заявки — в WMS и в другом продукте (Lupa), привязать уведомления и проверить доставку по каналам.

#### Шаги сценария по шагам

1. **Старт в «поддержке»:** пользователь в MAX (или Telegram) нажимает /start и входит в меню. Выбирает «Создать заявку».

2. **Первая заявка в WMS:** пользователь выбирает тип «Тип WMS» (из каталога). Core Support собирает поля по выбранному типу заявки (подразделение/WMS SD Department из справочника, процесс, настройки, комментарий). Пользователь заполняет и отправляет. По каталогу Core создаёт заявку в проекте Jira WMS (issue type и поля по каталогу), пишет в реестр «привязка по issue_key» и уведомляет. Сообщение пользователю: «Заявка создана: WMS-456, ожидайте».

3. **Вторая заявка в другом продукте (Lupa):** пользователь снова «Создать заявку» и выбирает тип «Поиск заявка / сайт» (Lupa). Core собирает поля (подразделение: число/подразделение, тип запроса, сервис, комментарий, город и т.д.). Пользователь заполняет и отправляет. По каталогу Core создаёт заявку в проекте Jira продукта (Lupa), пишет в реестр и уведомляет. Сообщение пользователю: «Заявка создана: LUP-789, ожидайте».

В реестре и в уведомлениях видны обе заявки: WMS-456 и LUP-789. Оба типа заявок созданы и в том же Core (единый каталог заявок), и в соответствующих проектах Jira и в реестре привязки.

#### Как привязка уведомлений работает по реестру

Привязка в Core идёт в Jira по каталогу, уведомления и привязка используют **единый реестр привязки** (channel_user_id, issue_key, project): при создании заявки WMS-456 и LUP-789 в реестр пишутся соответствующие привязки, а не по разным способам привязки заявок (WMS по имени в description, Lupa по tasks_users.json). То же привязка и в том, что уведомления уходят в канал (MAX или Telegram); привязка telegram_id и max_id выполняется по реестру привязки (см. п. 6.7).

- **Смена статуса:** при переходе в «В работе» и «Resolved» или иное — уведомление доставлено в MAX (и/или Telegram): «Заявка WMS-456 перешла в статус готово» (аналогично для LUP-789). Логика в Core; уведомления отправляются в канал.

- **Новый комментарий:** при добавлении комментария к заявке привязка по реестру даёт уведомление: «К заявке WMS-456 добавлен комментарий: текст а.»

- **Добавить комментарий:** пользователь нажимает «Добавить комментарий» к заявке WMS-456 или LUP-789 и отправляет текст комментария. Текст уходит в Jira через API и отображается в канале: «Комментарий по заявке WMS-456 от [канал]: [текст комментария пользователя]» (и то же для LUP-789).

- **Закрытие заявки:** при переходе в Done/Resolved отображаем «Заявка WMS-456 закрыта» (и при необходимости убираем из «Мои заявки» по реестру «мои заявки» в Core).

Остальные типы заявок аналогично; привязка по реестру сохраняется. Реестр привязки Jira хранится в Core (каталог, создание и привязка).

#### Ит по приёмочному сценарию по каналам

- **Мои заявки:** пользователь нажимает «Мои заявки». Core по реестру привязки (channel_id, user_id) возвращает список привязок (в том числе при совпадении одного пользователя в двух каналах) по всем продуктам (WMS, Lupa, Rubik). Отображаем список с ключами заявок (например, WMS-456, LUP-789).

- **Просмотр заявки:** по ключу заявки (например, WMS-456) в Core отображаем данные по заявке: статус, тип, комментарии, кнопка «Добавить комментарий» (данные не из Jira).

- **Добавить комментарий:** пользователь нажимает «Добавить комментарий» к заявке WMS-456, вводит текст. Запрос уходит в Core: «Добавить комментарий к issue WMS-456 от имени user_id». Core добавляет комментарий в Jira через API. После добавления комментария в Jira и при обновлении реестра уведомления уходят и в канал создания заявки и в канал добавления комментария (по реестру).

- **Резюме:** все шаги (создание заявки, «Мои заявки», смена пароля и т.д.) проходят через единый Core; привязка заявок хранится в реестре «мои заявки» и в привязке. Адаптеры (MAX, Telegram) только отображают меню и передают ввод в Core.

**Ит по приёмочному:** все шаги в «поддержке», все типы заявок по каталогу (WMS и Lupa), все заявки в едином реестре Jira; привязка уведомлений по реестру привязки, добавление комментария в том же реестре; доставка по привязке в блок «Мои заявки» и «Добавить комментарий». Всё это должно работать в Core Support и отображаться по каталогу MAX и (при наличии) Telegram.

---

"""
    write_part(TEMP_FILE, part4, "a")

    # Part 5: section 6.1-6.6
    part5 = """## 6. Детали по шести разделам

### 6.1 Подразделения в продуктах (department vs WMS SD Department)

- **Проблема:** в Rubik есть department (Jira AA, поле типа customfield_11406); в WMS используется отдельный справочник в Jira (customfield_18215 с отображаемым названием + department_key, например dep_murmansk). В Lupa нет отдельного справочника подразделений, используется subdivision из конфига (customfield_11406). Итог разный: WMS со своим справочником отделов (WMS_DEPARTMENTS), Rubik с department из Jira createmeta (AA).
- **Решения:**
  - Единый профиль пользователя в Core: поле `department` (общее для Rubik/Jira AA и т.д. в Lupa subdivision), а для WMS отдельно `wms_department_key` и `wms_department_display` (customfield_18215). Для отображения используем единый справочник полей.
  - **WMS-справочник только при первом типе заявки:** загрузка только при первом создании заявки типа WMS (справочник WMS_DEPARTMENTS из Jira или из конфига/админки) и отображение в сценарии создания (см. п. 2.2).
  - Порядок полей для всех продуктов: общие поля заполняются в общих справочниках DEPARTMENTS; продукты (WMS, Lupa, Rubik) и не дублируют друг друга при первом создании заявки по продукту.
  - При создании заявки поля маппятся в нужные поля Jira (WMS в customfield_18215 под wms_department_display; Rubik в JIRA_AA_FIELD_DEPARTMENT под department). Для WMS в каталоге отдельно, для «подразделения» справочник отдельно и в этом сценарии используется только для WMS (справочник-каталог по продукту WMS_DEPARTMENTS).

### 6.2 Jira: проекты, типы заявок, поля

- **Проблема:** три проекта (WMS в **PW**, поиск в **WHD**, Jira AA в **AA**), разные типы заявок и поля. WMS: три типа (wms_issue, wms_settings, psi_user), JSM + custom fields (18215, 13803, 10500, 18402 и т.д.). Lupa: WHD, Incident, custom 12312, 15800, 11406, 10500, 12403. Rubik: AA, Service Desk request type «Смена пароля», поля AD/phone/password/department. Поля не унифицированы.
- **Решения:**
  - В Core **каталог типов заявок по продуктам**: для каждого типа указываем project key (PW, WHD, AA, и т.д.), issue type / request_type_id (для JSM), маппинг полей (название + id поля в Jira). Тогда каталог-драйвер заявок, а не разброс по коду.
  - В каталоге для каждого типа задаём: project, issue_type (и при необходимости service_desk_id, request_type_id), маппинг полей с source (profile.department_display, profile.department, form.process, form.comment и т.д.) и target (customfield_XXXX). Различие department vs WMS SD Department задаётся в каталоге полей.
  - Все типы заявок каталогизированы для единого создания заявки и в том же реестре с тем же issue type и теми же полями.

### 6.3 Каталог типов заявок: 50+ типов, единый реестр и т.д.

- **Проблема:** не только 50 типов заявок; они должны быть в едином каталоге и в едином реестре привязки. Единый каталог и в том же виде для всех типов заявок и отображаются в меню по каталогу.
- **Решения:**
  - Каталог типов в конфиге (YAML/JSON): для каждого типа указываем id, отображаемое для UI, проект Jira, issue type, маппинг полей (имя поля, источник из профиля, тип, по каталогу; целевое поле в Jira field id). Дополнительно: уведомления (доставлять ли по типу заявки), доставка по каналу (max, telegram).
  - Core при «Создать заявку» читает каталог и в зависимости от выбранного типа и заполненного профиля по каталогу и полям создаёт issue по каталогу. Добавление 51-го типа = запись в каталог и в том же формате без изменения кода.
  - Реестр привязки единый и в том же формате (channel_user_id, issue_key, project), создание заявки и в реестре по тому же ключу. Для «мои заявки» Core отдаёт список по реестру.

### 6.4 Админы и права

- **Проблема:** в WMS main_admin + admin (users.json), в Lupa owner + admins (admins.json), в Rubik ADMIN_IDS (.env). Права заданы по-разному.
- **Решения:**
  - Унифицировать список админов по продуктам (admins WMS, admins Lupa, admins Rubik), но хранить в едином месте.
  - В объединённом боте для «админки» проверять по реестру прав: кто может просматривать заявки и в каком объёме с расширенными правами (подразделение WMS, Excel Lupa, смена подразделения Rubik и т.д.).
  - Важно, что проверка прав: файл `data/admins.json` и проверка по продукту для доступа к заявке и по реестру привязки прав для админки.

### 6.5 FSM и отмена сценария

- **Проблема:** в WMS, Lupa и Rubik есть пошаговые сценарии (FSM); при отмене в середине сценария нужно сбрасывать состояние и не оставлять половинчатых данных (как в текущей регистрации заявки WMS и сценарии «Смена пароля»).
- **Решения:**
  - Использовать отдельные namespace/ключ FSM по сценарию (например, `wms_create_ticket`, `lupa_create_ticket`, `rubik_change_password`), чтобы не пересекаться между типами заявок.
  - Команда отмены `/cancel` сбрасывает состояние в начале сценария сценария и выходит в главное меню без сохранения половинчатых данных, но сохранённые заявки остаются.
  - При отмене в середине сценария (например, «Смена пароля» по типу) предусмотреть: либо откат данных в сценарии FSM и отмена привязки по реестру сценария.

### 6.6 Уведомления и реестр заявок

- **Проблема:** в WMS и Lupa цикл опроса Jira раз в ~20 с (статусы + комментарии); в Rubik два цикла: опрос статуса по заявке (~90 с) и комментарии (~30 с). Реестр привязки заявок в разном виде: WMS по совпадению в description; Lupa по tasks_users.json; Rubik по pending_password_requests.json. В объединённом боте все уведомления должны доставляться по реестру, а способ доставки уведомлений унифицирован (в т.ч. для MAX).
- **Решения:**
  - В Core Support вести **единый реестр привязки** (user_id канала, issue_key, project, type_заявки). При создании заявки писать привязку; «мои заявки» и уведомления строятся по этому реестру. Не дублировать способы привязки «имя в description» (WMS) и отдельный JSON (Lupa, Rubik).
  - Два цикла опроса в одном event loop: один цикл для опроса Support (по проектам PW/WHD и каталогу заявок); отдельно Rubik (статус 90 с, комментарии 30 с) и оба циклы в том же процессе в том же реестре привязки, если заявка из канала.
  - Доставка в MAX: отправка по user_id канала MAX (chat_id = user_id в MAX API). Привязка telegram_id и max_id выполняется по реестру привязки (см. п. 6.7).
  - Ит в реестре: тип заявки (wms/lupa/rubik) и канал.

### 6.7 Идентификация пользователя: Telegram vs MAX

- **Проблема:** в Telegram в трёх ботах (WMS, Lupa, Rubik), в каждом канале telegram_id свой; в MAX один канал, там max user_id. Один человек может пользоваться и Telegram-ботом (например, смена Rubik), и тем же в MAX.
- **Вопрос:** в Telegram идентификация идёт по (bot_token/приложение + telegram_id); в MAX по max user_id. Как связать пользователя в Telegram-боте, в MAX-боте, чтобы не дублировать «мои заявки» (один человек, один реестр)?
- **Решения:**
  - Вариант 1: в MAX при регистрации запрашивать телефон и привязывать по номеру (поиск профиля по max_id); «мои заявки» в MAX отображают заявки, созданные в MAX. Тогда не требуется привязка к Telegram, только к MAX.
  - Вариант 2: при первом входе в MAX показывать «Привязать к аккаунту Telegram» (например, код или по Telegram-боту); тогда храним пару (telegram_id, max_id), «мои заявки» общие. Подробнее в п. 6.7а.
  - Единая привязка в реестре и в профиле (хранение/обновление привязки telegram_id и max_id).

"""
    write_part(TEMP_FILE, part5, "a")
    write_part(TEMP_FILE, SECTION_6_7A, "a")

    # Part 6: 6.8-6.11, 7, 8, 9, 10
    part6 = """### 6.8 Справочники и конфигурация

- **Проблема:** в Lupa есть справочники (подразделение, тип заявки, приложение, сервис), в WMS свои (процесс, настройки), в Rubik свои (типы Jira AA). Все справочники должны быть единообразно доступны в Core по продукту.
- **Решения:**
  - Загрузка справочников по продукту (config или отдельный JSON), а не в коде «жёстко-зашито».
  - В каталоге заявок указываем источник по продукту и полю, откуда брать значения (Jira, конфиг, department, wms_sd_department и т.д.); при создании заявки справочник подгружается по продукту и отображается в адаптере.

### 6.9 Хранение профилей пользователей

- **Проблема:** в WMS и Lupa два формата хранения (users.json с department/department_key; user_data.json с subdivision, employee_id и т.д.) и разная структура. В Rubik user_data.json + индексы по логину/email/телефону.
- **Решения:**
  - Унифицировать формат профиля в Core (department, department_key, department_display и общие поля: name, phone, email и т.д.).
  - Миграция данных при переносе: для существующих пользователей из WMS (users.json) и Lupa (user_data.json), Rubik (user_data.json) маппить в единый формат Core; отображаемые department_display/department_key для WMS, subdivision для Lupa, department для Rubik (при необходимости обратная совместимость). Единый реестр и единый профиль.

### 6.10 Единый сценарий отмены и ошибок

- **Проблема:** три бота по-разному обрабатывают ошибки (например, «Регистрация недоступна в этом боте Lupa» или «Доступ Rubik только по логину»).
- **Решения:**
  - Единый сценарий обработки по каталогу заявок: сообщения об ошибках, коды FSM, кнопка-отмена, уведомления. Убедиться, что все сценарии обрабатываются в объединённом support-боте (MAX/Telegram) или по продукту отдельно.
  - Тексты сообщений хранить в конфиге «единообразно в одном месте» (например, ошибка типа WMS и текст в том же блоке и т.д.) и в FSM по сценарию не дублировать.

### 6.11 «Мои заявки» и реестр привязки и доставка в Jira

- **Проблема:** в текущих трёх ботах «мои заявки» и «кто создал заявку» привязаны по-разному: WMS по совпадению в description + label «поддержка»; Lupa по tasks_users.json (issue_key → user_id); Rubik по pending_password_requests.json. Для объединённого и при доставке MAX нужно единое: по (channel, user_id) доставлять во все заявки и уведомления в тот же канал (MAX или Telegram).
- **Решения:**
  - В Core Support **единый реестр привязки заявок** (таблица или аналог): при создании заявки пишем (channel_id: "max"|"telegram", user_id, issue_key, project_key, ticket_type, created_at). «Мои заявки» по запросу по (channel_id, user_id). Уведомления по issue_key доставляем (channel_id, user_id) и отправляем в соответствующий канал (MAX или Telegram).
  - При миграции: для существующих заявок WMS (PW) и Lupa (WHD) по текущим способам привязки (совпадение в description, tasks_users.json) либо мигрируем в единый реестр, либо читаем оба источника при отображении. То же в обратном порядке при создании заявки.
  - Для Rubik пока оставляем как есть по типу заявки «rubik_password», т.е. при создании заявки пишем в pending_password_requests и привязку с channel_id/user_id, то уведомления доставляем в MAX или созданную заявку по MAX.

---

## 7. Порядок работ (приоритизация)

Два порядка работ рассматриваем в плане:
- **A. Полный порядок** (объединение WMS+Lupa+Rubik).
- **B. Поэтапный порядок от репозитория `the_bot_rubik`** (MVP в виде Core+адаптеры в текущем репозитории).

### 7.A Порядок работ (полный план: WMS + Lupa + Rubik)

1. **Core и каталог заявок:** перенести логику Support (WMS, Lupa, Rubik) в `core/`; определить единый API Core (меню для адаптеров: создание заявки, типы, «мои заявки», уведомления и т.д.) для вызова из адаптеров.
2. **Каталог типов заявок:** описать в конфиге (YAML/JSON) каталог типов: WMS (PW и wms_issue, wms_settings, psi_user), Lupa (WHD, Incident), Rubik (AA, смена пароля), и общие поля (department_display и customfield_18215 для WMS; department для AA; поля Lupa по каталогу). Добавить остальные 50+ типов (или не все сразу).
3. **Core и единый реестр привязки заявок:** при создании заявки в Core: по типу заявки из каталога заполняем профиль и поля по каталогу и создаём issue в Jira по каталогу. При создании пишем привязку в **единый реестр привязки** (channel_id, user_id, issue_key, project, type). Уведомления/комментарии = реестр привязки.
4. **Справочники и поля в Core:** все справочники (department, wms_sd_department и т.д.), единые поля для всех типов заявок в «профиле»; загрузка справочников по продукту и полю в Core.
5. **Адаптер MAX и запуск:** подключить MAX-бот по токену (например `MAX_BOT_TOKEN`), обработчики, long polling.
6. **Адаптер MAX и Support:** вызывать логику Core Support; отображать меню «Создать заявку» по каталогу типов; «мои заявки», смена пароля по реестру в Core.
7. **Циклы уведомлений и привязка в Core:** единый реестр для всех типов «мои заявки»; циклы уведомлений (как сейчас по типам в Jira и/или интервал опроса Rubik 90/30 с) по реестру привязки; доставка в MAX и Telegram по реестру.
8. **Миграция ботов:** перенести сценарии регистрации WMS/Lupa/Rubik в адаптеры и вызывать Core; проверить сценарии.
9. **Адаптер Telegram (опционально):** тот же Core Support, подключить адаптеры Telegram в том же формате меню тикетов что и в MAX, и в Telegram.
10. **Приёмочные тесты:** выполнить по сценарию приёмки из раздела 5; убедиться, что все типы заявок, уведомления и комментарии работают по реестру и отображаются в каталоге типов Jira по каталогу заявок.

### 7.B Порядок работ (поэтапный: старт от репозитория `the_bot_rubik` и переход на Core+адаптеры)

Текущий репо `the_bot_rubik` уже содержит зачатки ядра, которые нужно вынести в Core и адаптировать:
- `core/registration.py`, `user_storage.py` и реестр с привязкой (уже есть привязка по телефону для MAX).
- `core/jira_aa.py` и создание заявки/уведомления/комментарии по Jira AA.
- `core/password_requests.py` и реестр привязки заявок и уведомления (текущий реестр `issue_key -> telegram user_id` в `data/pending_password_requests.json`).
- `handlers/*`, `keyboards.py`, `states.py` — Telegram-обработчики на aiogram (текущие сценарии и UI остаются точкой входа).

Далее в порядке приоритета ниже.

#### Этап 1: MVP «Core API» для Telegram/MAX-адаптеров
- **Цель:** выделить единый API Core, не зависящий от aiogram, с абстракциями ответов: текст, меню заявок, форма заявки, ошибка.
- **Результат:** Telegram-адаптер вызывает только этот API (единый) и преобразует ответы в формат Core.
- **Шаги:**
  - Создать `core/api.py` (или `core/support/api.py`) с методами вызова (например: start/menu, registration, create_ticket, my_tickets, add_comment).
  - Ввести абстракции ответов Core (DTO), например: `Text`, `Menu(buttons)`, `Form(fields)`, `Error`.

#### Этап 2: Каталог типов заявок (минимум 1 тип: Rubik смена пароля)
- **Цель:** сделать каталог-driven сценарий, где тип заявки = запись в YAML/JSON.
- **Результат:** один тип заявки уже описан в каталоге (как минимум один сценарий).
- **Шаги:**
  - Добавить `config/ticket_catalog.yaml` с первым типом `rubik_password_change` (проект AA, request type id, поля заявки).
  - Подключить Core так, чтобы тип заявки из каталога подставлялся в сценарий создания заявки.

#### Этап 3: Единый реестр привязки заявок (замена `pending_password_requests.json`)
- **Цель:** перейти на единый реестр: привязка и уведомления по реестру, MAX и Telegram доставляют уведомления.
- **Результат:** один реестр и один формат привязки уведомлений.
- **Шаги:**
  - Создать единый реестр привязки (JSON или таблица или SQLite и т.д. см. решение 9), с полями:
    `channel_id`, `channel_user_id`, `issue_key`, `project_key`, `ticket_type_id`, `created_at`, (опционально) `last_seen_status`, `last_seen_comment_id/count`.
  - При создании заявки писать привязку в реестр.
  - Мигрировать/заменить текущий формат `data/pending_password_requests.json` (или читать из обоих при переходе).

#### Этап 4: Два канала + уведомления по реестру
- **Цель:** доставлять уведомления/комментарии по единому реестру привязки заявок.
- **Результат:** пользователь видит свои заявки и уведомления по реестру в обоих каналах.

#### Этап 5: Уведомления (Core опрашивает Jira и реестр, доставка в оба канала)
- **Цель:** реализовать уведомления универсально: Core опрашивает Jira по реестру привязки, доставка.
- **Результат:** и в Telegram и в MAX приходят уведомления по заявкам.
- **Шаги:**
  - Вынести логику опроса из `core/password_requests.py` и перенести в «delivery» (единый/раздельный), Core не зависит от aiogram.
  - Реализовать доставку уведомлений (статус/комментарий) в оба канала, а не в pending-реестр.

#### Этап 6: Отдельный MAX-адаптер (подключение)
- **Цель:** чтобы бот MAX подключался к тому же Core.
- **Результат:** единый (или раздельный по продукту), создание заявки единое, «мои заявки», уведомления.

#### Этап 7: Полный каталог и миграция Core+adapters
- **Цель:** все три продукта в той же структуре 3 (Core ядро, два адаптера).
- **Результат:** объединённые WMS/Lupa сценарии переносятся в core/адаптеры, либо подключаются по API.

---

## 8. Критерии приёмки

- [ ] **Core и адаптеры:** все три support-бота Support в Core; MAX и Telegram как адаптеры, вызывают Core. Единое меню типов заявок в каталоге по каталогу типов заявок создаётся.
- [ ] Единая регистрация по шести шагам обязательна для WMS, Lupa и Rubik (и при создании заявки в том же сценарии в том же реестре по каталогу типов заявок) в одном виде и в MAX и/или Telegram.
- [ ] Унификация полей в профиле department и wms_sd_department; справочники загружаются по продукту по каталогу типов заявок.
- [ ] Создание заявки по каталогу по типу и по привязке.
- [ ] FSM по сценарию/типу заявки сбрасывается, /cancel и сохранённые заявки в реестре привязки создаются.
- [ ] Уведомления по Jira доставляются по реестру и в MAX (и в Telegram при наличии привязки); привязка по единому реестру привязки (channel_id, user_id, issue_key).
- [ ] «Мои заявки» строятся по реестру привязки; при создании заявки привязка записывается в реестр.
- [ ] Каталог типов заявок каталогизирован; единый реестр привязки заявок создаётся по каталогу (в т.ч. не 50+ а по мере добавления) и единый вид Core и адаптеры.

---

*Критерии приёмки заданы по разделам приоритизации и каталогу.*

---

## 9. Решения, которые влияют на реализацию (кратко без детализации)

### 9.1 Привязка одного пользователя в каналах Telegram и MAX

См. п. 6.7 и п. 6.7а с детальным сценарием, т.е. не только «как привязать» но и порядок:
- **Вариант 1 (телефон):** привязка по номеру телефона в обоих каналах (Telegram и MAX).
- **Вариант 2 (код привязки):** хранение `telegram_user_id и max_user_id` (по коду/привязке), тогда реестр и в том же виде заявки общие.

Рекомендация на данный момент: идти в **вариант 1**, пока не потребуется в будущем виде `linked_id`/единый идентификатор, тогда перейти на вариант 2 или доработать привязку.

### 9.2 Выбор хранилища реестра привязки

- **JSON (удобно для MVP):** простой формат, но при росте объёма — блокировки и ограничения при конкурентной записи.
- **SQLite (предпочтительно при росте объёма заявок):** надёжнее для единого реестра, но нужна миграция, реестр и в том же формате привязки реестр.

Рекомендация: начать с хранения реестра JSON на первом этапе, при **росте нагрузки** перейти на SQLite (или решение по SQLite см. решение 3).

### 9.3 Формат ответов API/адаптеров Core

Поскольку Core не знает про детали UI (aiogram/MAX), ответы должны быть абстрагированы:
- текст/сообщение (или rich-text для уведомлений),
- меню заявок (id, label),
- форма заявки (поля: text/select/contact/file),
- кнопки отмены (как для всех сценариев отмены).

Остальное (inline-кнопки Telegram, формат MAX и т.д.) — в адаптере.

---

## 10. Backlog по `the_bot_rubik` (приоритет по срочности/важности, не по срокам)

Список в виде бэклога задач/идей приоритизации к переносу в Core+адаптеры.

### 10.1 Структура модулей/файлов
- `core/support/`:
  - `api.py` с единым API Core (вызовы для адаптеров).
  - `models.py` с DTO ответов Core (Text/Menu/Form/Error и т.д.).
  - `ticket_catalog.py` с загрузкой/чтением каталога типов заявок.
  - `issue_binding_registry.py` — реестр привязки (JSON/SQLite).
  - `notifications.py` — опрос Jira + доставка уведомлений (по реестру привязки).
- `config/`:
  - `ticket_catalog.yaml` с каталогом типов заявок (минимум с `rubik_password_change`).
  - (опционально) `profile_schema.yaml` с полями профиля и маппингом в Jira/продукты.
- `adapters/telegram/`:
  - `main_tg.py` — точка входа Telegram.
  - `handlers/` — тонкие обработчики (вызов Core API + преобразование DTO в aiogram).
- `adapters/max/`:
  - `main_max.py`, `handlers/` с вызовами MAX-адаптера.

### 10.2 Задачи, которые стоит выполнить параллельно (бэклог)
- Перенести `handlers/*`, `keyboards.py`, `states.py` и т.д. в вызовы к `adapters/telegram/`.
- Перенести `core/jira_aa.py` в общий клиент Core-обращения к Jira, не дублировать логику вызова `core/support/api.py`.
- Перенести `core/password_requests.py` в:
  - «registry + notifier events» в Core,
  - «delivery» в адаптеры.


---

"""
    write_part(TEMP_FILE, part6, "a")

    # Read section 11 from APPEND
    with open(APPEND_FILE, "r", encoding="utf-8") as f:
        append_content = f.read()
    # Section 11 starts with "---" then "## 11" in APPEND
    if "## 11." in append_content:
        idx = append_content.index("## 11.")
        section11 = append_content[idx:]
    else:
        section11 = append_content.strip()

    # Read all restored content (parts 1-6)
    with open(TEMP_FILE, "r", encoding="utf-8") as f:
        restored = f.read()

    # Final document: restored (ends with --- and newline) + section 11
    out = restored + section11

    # Write to PLAN_UNIFIED_MAX_BOT.md as UTF-8 (no BOM)
    with open(OUT_FILE, "w", encoding="utf-8", newline="\n") as f:
        f.write(out)

    # Clean up temp file
    try:
        os.remove(TEMP_FILE)
    except OSError:
        pass

    print("OK: PLAN_UNIFIED_MAX_BOT.md written (UTF-8, no BOM).")


if __name__ == "__main__":
    main()
